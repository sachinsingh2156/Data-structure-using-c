#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n, i, *ptr;
    printf("\nENTER THE NUMBER OF USN WANT TO ENTER->");
    scant("%d", &n);
    *ptr = (int *)malloc(n * sizeof(int));
    if (ptr == NULL)

        printf("\n YOUR MEMORY IS FULL");
    printf("\n ENTER THE USN->");
    for (i - 0; i < n; i, +)
        scanf("%d", ptr - 1);

printf("\n THE USN ENTERED ARE-> ;
for (1=0;1<n;1++)
printf ("xd\", "(ptr+i));
return 8;
